using System;
using UnityEngine;

public class ColorChanger : MonoBehaviour
{
    // Objetivo: ouvir o evento de "mudança de estado de boolean do inspector", disparado pelo InspectionManager
    // Processar mudança de cor do objeto passado pelo InspectionManager

    private Renderer _renderer;
    private Color _color;

    private void Start()
    {
        // pegar referencia do Renderer Color com GetComponent
    }

    private void Update()
    {
        
    // usar Enumerator para fazer a mudança de cor do objeto
        
        
    }
    
}
